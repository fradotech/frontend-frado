/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'placehold.it',
      'via.placeholder.com',
      'frado.onrender.com',
      'lh3.googleusercontent.com',
    ],
  },
}

export default nextConfig
